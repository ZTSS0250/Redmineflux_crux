# backlog/planning

Task files land here first, named `crx-{NNN}-{type}-{description}.md`, using the task file template in the global `CLAUDE.md`. A task stays here through the Planning section (developer-filled) and the Specification + three-gate quality review (Claude-filled). Once all three gates are Approved, the file moves to `../specification/`.
