# backlog/specification

Task files land here once all three quality gates (Senior Developer, Security & Performance, Pre-Development Sweep) are Approved — cleared for implementation. Developer implements the Code Changes and runs the Test Cases recorded in the file. Once every test case passes and the PR merges, the file moves to `../done/`.
