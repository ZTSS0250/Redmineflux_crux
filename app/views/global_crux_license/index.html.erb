<% content_for :sidebar do %>
  <%= render 'crux_global_sidebar/global_crux_sidebar' %>
<% end %>
<%= stylesheet_link_tag('crux_dashboard', plugin: 'redmineflux_crux') %>

<h2><%= l(:label_license, :scope => :crux) %></h2>
<span class="crux-demo-notice">Demo preview — not live data</span>

<div class="crux-stats">
  <div class="crux-stat-tile">
    <span class="crux-stat-value"><%= @plan_tier %></span>
    <span class="crux-stat-label">Plan Tier</span>
  </div>
  <div class="crux-stat-tile">
    <span class="crux-stat-value"><%= @seats[:used] %> / <%= @seats[:total] %></span>
    <span class="crux-stat-label">Seats Used</span>
  </div>
</div>
